{
  "name": "project-clock",
  "version": "0.1.0",
  "description": "A simple CLI app for clocking time spent on a project.",
  "main": "bin/pclock.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jari Ikonen",
  "bin": {
    "pclock": "./bin/pclock.js"
  }
}
